# 版本日志

本文件记录每个开发版和正式版的用户可见变化。版本规则见 [docs/versioning.md](docs/versioning.md)。

## 开发版 0.1.22 — 2026-08-26

- 在 README 中加入 PDF 导入、目录识别和书签编辑三张界面截图，并限制展示宽度以保持页面易读。
- 调整使用步骤说明，使其与截图展示的导入、AI 解析、页码映射和书签检查流程一致。

## 开发版 0.1.21 — 2026-08-25

- 为 Windows `setup.exe` 增加专用安装器图标：保留应用的紫色书本主体，并在右下角加入白紫色“向下安装到托盘”徽标，小尺寸下可与应用 EXE 区分。
- 应用本体和卸载程序继续使用原应用图标，不受安装器图标修改影响。

## 开发版 0.1.20 — 2026-08-25

- 将 PDF 文字选择层切换为 PDF.js 官方 `TextLayerBuilder`，修复跨行、跨页拖选时选区端点和辅助节点未正确复位的问题。
- 补齐隐藏换行不着色、装饰图标不可选择、标记内容嵌套和选择结束清理规则，消除页面最左侧出现的无关紫色短条。
- 降低文字选区背景不透明度，使中文字体完整行框的上下边界更轻，减少选区视觉膨胀，同时保留准确的文字定位和复制范围。

## 开发版 0.1.19 — 2026-08-25

- 修复包含 JPEG 2000（`JPXDecode`）整页图像的 PDF 页面无法渲染的问题：随应用打包 PDF.js 的 OpenJPEG WASM 与无 WASM 降级解码器，并在加载文档时显式配置解码资源路径。
- 同步打包 JBIG2 与 QCMS WASM 资源及对应许可证，避免同类特殊图像和 ICC 色彩空间页面因缺少运行时解码资源而失败。

## 开发版 0.1.18 — 2026-08-25

- 为 PDF 阅读区增加链接标注层，链接热点会随页面适宽、缩放和旋转后的视口坐标同步更新，并支持鼠标与键盘访问。
- 支持点击 PDF 内部链接跳转到目标页，以及首页、末页、上一页和下一页等命名跳转。
- PDF 中的 HTTP/HTTPS 外部链接现在交给系统默认浏览器打开；通过 Tauri opener 后端严格拒绝文件、脚本等非网页协议。

## 开发版 0.1.17 — 2026-08-25

- 将 Windows 安装器侧栏和顶部横幅改为与应用内一致的皇家紫、亮紫高光、白色书页和紫色书签主题，移除原有靛蓝与珊瑚色元素。
- 安装与卸载程序改为直接使用应用图标，使安装包、安装向导和应用本体的品牌配色保持一致。

## 开发版 0.1.16 — 2026-08-25

- 按用户澄清撤回 0.1.15 对应用本体图标和主界面的视觉修改，恢复原有应用视觉；起始页“原文件保持不变”仍按要求删除。
- 重新设计 Windows 安装器：增加安装器专用图标、靛蓝品牌侧栏、顶部横幅，以及定制的中文欢迎页和完成页文案。
- 安装程序文件名继续保持英文 `BookmarkCraftsman_0.1.0_x64-setup.exe`，中文向导和英文默认安装目录保持不变。

## 开发版 0.1.15 — 2026-08-25

- 重新设计应用图标，以靛蓝底、白色书页和珊瑚色书签形成更清晰的 Windows 小尺寸识别效果，并同步更新应用内、安装包和系统图标资源。
- 统一主界面为更现代的靛蓝视觉体系，优化顶部栏、设置区、按钮、起始卡片、缩略图、书签面板、阅读工具栏、弹窗和状态栏的层级、圆角、间距与阴影。
- 删除起始页面的“原文件保持不变”提示文字。

## 开发版 0.1.14 — 2026-08-25

- 将 Windows 安装程序的 EXE 文件名改为英文 `BookmarkCraftsman_0.1.0_x64-setup.exe`；中文安装向导、应用名称、快捷方式和卸载项仍显示“书签匠”。

## 开发版 0.1.13 — 2026-08-25

- 精简 README，聚焦 PDF 预览、目录识别、页码映射、书签编辑和安全导出等主要功能。
- 将 Windows NSIS 安装与卸载向导固定为简体中文，同时保留应用显示名“书签匠”。
- 将新安装的默认目录改为纯英文 `%LOCALAPPDATA%\BookmarkCraftsman`；已有安装升级时仍沿用原目录。

## 开发版 0.1.12 — 2026-08-25

- 修复修改页码映射参数后直接导出仍使用旧目标页的问题；导出带书签 PDF 前会强制按当前“印刷页 / PDF 页”参数重新计算全部书签，并将同一结果同步回编辑列表。

## 开发版 0.1.11 — 2026-08-25

- 全面更新书签匠 Logo：采用紫色圆角底、展开书页与中央书签带组成的新图形，在 32×32 小尺寸下仍保持清晰辨识。
- 同步替换 Windows 可执行文件、安装包、任务栏、启动过渡、左上角入口、状态栏和版本弹窗中的品牌图标。

## 开发版 0.1.10 — 2026-08-20

- 提升书签跳转响应：目标页进入视口后会优先于附近页面进入正文渲染队列。
- 修复跳转到较后页码后，附近页面完成尺寸计算导致视口回落一页的问题；跳转期间会跟随页面布局变化校正位置，并保持工具栏页码与实际页面一致。
- 用户主动滚轮、拖动或缩放时立即结束自动定位，避免跳转校正干扰正常阅读。

## 开发版 0.1.9 — 2026-08-20

- 将左上角 Logo 弹窗精简为纯版本信息，只保留当前开发版本、正式基线和版本通道。
- 导出前分别检查目录页范围与页码映射；任意一项仍为初始的 1–1 时都会弹窗提示，不再只有两项都未修改时才提醒。

## 开发版 0.1.8 — 2026-08-16

- 修复缩放和滚动后正文页偶发全黑：页面改为在新画布完整渲染成功后原子替换旧画布，释放或重绘期间保持白色纸张背景。
- 增加正文页串行渲染队列、画布像素与边长限制，以及渲染失败时的降清晰度自动重试，降低 WebView2 Canvas/GPU 内存峰值。
- 重绘失败时保留上一张正常页面并显示可重试错误，不再先清空显示画布或静默留下黑页。

## 开发版 0.1.7 — 2026-08-16

- 增加应用启动加载过渡：WebView 首屏立即显示“书签匠”品牌标识和动态进度，主界面首帧完成后平滑淡入，避免启动阶段出现突兀白屏。
- 将原生窗口和 WebView 的初始背景统一为应用浅紫底色，进一步减少 HTML 加载前的颜色跳变。

## 开发版 0.1.6 — 2026-08-14

- 重构 PDF 文档切换生命周期：新文档加载成功后完整重建阅读区与缩略图列表，再释放旧文档，修复左侧仍显示上一份 PDF 的问题。
- 参考 SumatraPDF 的按倍率渲染与旧位图替换机制，缩放时按目标倍率重新生成可见页画布；新画布完成前保留旧画布，文字型 PDF 放大后保持清晰且不再闪白。
- 阅读区只渲染视口附近页面，离开附近区域后释放高分辨率画布，并限制单页画布像素量，降低长文档连续阅读时的内存占用与白页风险。
- 文档切换和页面卸载时取消过期的缩略图与正文渲染任务，避免旧任务覆盖新 PDF 预览。

## 开发版 0.1.5 — 2026-08-14

- PDF 连续阅读区改为整段文档统一缩放，修复快速放大或缩小时偶发单页未同步的问题。
- 缩小连续 PDF 页面之间的留白间距，让上下阅读衔接更紧凑。

## 开发版 0.1.4 — 2026-08-07

- 目录页输入改为编辑完成后校验，允许先清空再输入；失焦或回车后才提交并处理范围关系。
- PDF 缩放改为即时视觉缩放，不再因每次缩放重复渲染页面；`Ctrl + 滚轮`以鼠标位置为锚点补偿滚动。
- 阅读区页码统一字号与常规字重；输入跳页后，左侧缩略图列表自动滚动到对应页面。
- 删除带子级的父书签时提供“全部删除”或“保留并提升一级”选择。
- 插入热点与书签行不再同时高亮，加号改为更大的圆头样式；统一每行书签的上下边距。
- 书签右键菜单增加“全部展开”，并缩短拖动距离阈值、落点阈值和冷却时间。
- 优化目录识别提示词：无页码内容默认不返回，仅在维持有效层级所必需时保留无页码父级。
- 界面不再显示文本型、扫描型或混合型 PDF 分类。

## 开发版 0.1.3 — 2026-08-07

- 修复自带书签在应用页码映射后丢失目标页的问题；没有印刷页码的书签保留原 PDF 跳转页。
- 未设置 API 时，点击 AI 解析会显示应用内引导弹窗，可直接前往设置，且不会触发 Windows 通知。
- 将“导入 JSON”放在 API 设置左侧，并精简“目录页”“页码映射”命名和页码输入框宽度。
- PDF 阅读区增加缩放按钮、适合宽度复位、`Ctrl + + / - / 0` 和 `Ctrl + 滚轮`缩放。
- 阅读区页码支持单击输入并跳转，左侧缩略图页码改为偏灰的半透明紫色。
- 目录起始页超过结束页时立即同步结束页；结束页小于起始页时立即拒绝输入、恢复旧值并提示。

## 开发版 0.1.2 — 2026-08-07

- 统一所有层级书签的全宽分隔线，缩小层级缩进，并将插入提示简化为左侧低对比度加号。
- 增加多层级书签的展开/折叠；移动、调级、删除和间隙插入均保持父子书签关系。
- 拖动改为逐帧检测、跨越落点阈值、同级排序和短暂冷却，抑制高频往返抖动。
- 增加“清空”按钮和应用内二次确认，清空后仍可撤销恢复。
- 页码输入框取消外扩焦点效果并收紧尺寸，避免和删除按钮重叠。
- 修正 API 与网页 AI 提示词：保留单独出现的“目录/Contents”页面主标题，仅忽略重复页眉、页脚和纯装饰元素。

## 开发版 0.1.1 — 2026-08-07

- 建立开发版与正式版双轨版本规则，开发版号显示在应用“关于”窗口。
- 页码改为单击进入编辑，并扩大输入区域，避免与操作按钮重叠。
- 移除书签行内的加号；鼠标移动到书签间隙时显示添加引导，单击即可插入书签。
- 目录范围与页码映射均保持初始 `1–1` 时，导出目录 PDF 或带书签 PDF 前进行二次确认。

## 正式版 0.1.0 — 2026-08-07

- 建立 Windows 桌面应用基线：PDF 预览、目录多模态识别、页码映射、书签编辑与 PDF 导出。
- 支持 API 解析失败时改用网页 AI，以及目录页高清图片回退、进度、Token、耗时和应用通知。
- 完成紫白主题、紧凑状态栏、书签右键操作和独立 API 设置窗口。
